# 糯叽机 ⇄ Float 备份转换器 V4

本目录是 GitHub Pages 平铺上传版，没有子文件夹。

上传方法：解压 ZIP，在 GitHub 仓库根目录选择 Add file → Upload files，然后一次选中本目录全部文件并提交。

支持糯叽机 `.gz` → Float `.zip`，以及 Float `.zip` → 糯叽机 `.gz`。Float 剧情和线下记录按角色建立独立分页，不会混合。
